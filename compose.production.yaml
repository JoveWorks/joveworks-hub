services:
  hub:
    build: .
    env_file: .env
    ports: ["${JOVEWORKS_HOST_BIND:-127.0.0.1}:8080:8080"]
    volumes: ["joveworks-data:/var/lib/joveworks"]
    restart: unless-stopped
volumes:
  joveworks-data:
